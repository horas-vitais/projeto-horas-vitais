import styled from "styled-components";

const Form = styled.form`
    display: flex;
    
    input {
        background-color: rgba(0,0,0,0.1);
    }

    button {
        width: 50%;
    }
`
export { Form }